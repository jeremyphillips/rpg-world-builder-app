import type { BenchColumn } from '@rpg/dev-bench-core'

export const BENCH_COLUMN_EMPTY_COPY: Record<BenchColumn, string> = {
  up_next: 'No tickets up next',
  in_progress: 'Nothing in progress',
  blocked: 'No blocked tickets',
  done: 'Nothing done yet',
}
