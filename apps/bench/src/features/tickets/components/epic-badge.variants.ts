/** Static badge when a ticket has no epic assigned. */
export const epicBadgeNoEpicClasses = 'border-transparent bg-muted text-muted-foreground'

/** Link wrapper for epic badges on interactive ticket cards. */
export const epicBadgeLinkClasses =
  'inline-flex max-w-full rounded-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 focus-visible:ring-offset-background'
