import { pageSpacingClasses, type PageSpacing } from './page-spacing.variants'

/** Centered narrow-column page shell shared by settings, forms, and stub routes. */
export const narrowPageBaseClasses = 'mx-auto w-full min-w-0 max-w-4xl'

export { pageSpacingClasses as narrowPageSpacingClasses, type PageSpacing as NarrowPageSpacing }
