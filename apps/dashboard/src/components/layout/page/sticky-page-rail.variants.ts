/** Reusable stretch slot for sticky side rails in page-scroll layouts. */
export const stickyPageRailSlotClasses = 'shrink-0 self-stretch'

/** Sticky inner panel inside a full-height rail slot. */
export const stickyPageRailPanelClasses = 'sticky top-0'
