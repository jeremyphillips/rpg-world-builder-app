import { pageSpacingClasses, type PageSpacing } from './page-spacing.variants'

/** Full-width page shell — uses the main column width from AppShell. */
export const widePageBaseClasses = 'w-full min-w-0'

export { pageSpacingClasses as widePageSpacingClasses, type PageSpacing as WidePageSpacing }
