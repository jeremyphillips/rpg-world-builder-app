# user (dashboard feature)

> Scaffold only — no implementation yet.

The signed-in user's own profile and account preferences inside the DM
workspace (display name, avatar, settings). Distinct from `auth`, which only
manages the session, and from the API's `user` domain model.

Folder layout and the feature-boundary rule are documented in
[feature-conventions](../../../docs/feature-conventions.md).
