import { cn } from '@rpg/ui'

export const featureTableRowShellClasses = cn('rounded-md border border-border px-3 py-2.5')

export const featureTableRowLayoutClasses = 'flex min-w-0 items-center gap-3'

export const featureTableRowCopyClasses = 'flex min-w-0 flex-1 flex-col gap-0.5'

export const featureTableRowTitleClasses = 'truncate font-medium text-foreground'

export const featureTableRowMetadataClasses = 'text-xs text-muted-foreground'

export const featureTableRowTrailingClasses = 'flex shrink-0 items-center gap-4'

export const featureTableRowActionsClasses = 'flex shrink-0 items-center gap-3'
