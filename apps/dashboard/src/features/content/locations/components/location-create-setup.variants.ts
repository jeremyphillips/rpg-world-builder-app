/**
 * Modal body spacing:
 * - 16px between choice-set sections (summary ↔ summary ↔ expanded field)
 * - 16px between an expanded field’s label and radio list
 */
export const locationCreateSetupModalBodyClasses =
  'flex flex-col gap-4 [&_[data-field-align]]:gap-y-4'
