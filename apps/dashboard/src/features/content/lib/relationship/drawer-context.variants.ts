import { cva } from 'class-variance-authority'

export const drawerContextVariants = cva('space-y-2')

/** Host spacing for embedded drawer-context entity identity blocks. */
export const drawerContextEntityVariants = cva('min-w-0')
