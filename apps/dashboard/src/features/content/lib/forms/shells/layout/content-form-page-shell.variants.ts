/** Inline breadcrumb row inside the form column — mirrors shell rail border treatment. */
export const contentFormInlineBreadcrumbClasses = 'border-b border-border pb-3'

/** Fills app main below the breadcrumb rail so TabbedForm can dock its footer. */
export const contentFormPageShellClasses = 'flex min-h-0 flex-1 flex-col overflow-hidden'

/** Page heading row above the form — keep out of the bounded scroll column. */
export const contentFormPageShellHeadingClasses = 'shrink-0'
