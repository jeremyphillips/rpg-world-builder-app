/** Dashed inset gate — shared by columns and preview empty states. */
export const tableBuilderInsetGatePanelClasses = 'flex flex-col items-center'

export const tableBuilderInsetGateIntroClasses = 'flex flex-col items-center gap-2'

export const tableBuilderInsetGateIconClasses = 'size-8 text-muted-foreground'

/** 8px between optional heading and supporting copy. */
export const tableBuilderInsetGateCopyClasses = 'flex flex-col items-center gap-2'

/** 16px gate heading. */
export const tableBuilderInsetGateHeadlineClasses = 'text-base font-medium text-foreground'

/** 14px gate supporting copy. */
export const tableBuilderInsetGateDescriptionClasses = 'text-sm text-muted-foreground'
