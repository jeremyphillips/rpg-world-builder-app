# message (dashboard feature)

> Scaffold only — no implementation yet.

Direct and campaign-scoped messaging between users. Realtime/websocket delivery
is deferred; the first pass will be request/response over the same-origin API.

Folder layout and the feature-boundary rule are documented in
[feature-conventions](../../../docs/feature-conventions.md).
