/** Preview cap per filter group on the full results page (`group=all`). */
export const PAGE_GROUP_PREVIEW_LIMIT = 6

/** Preview cap per filter group in the topbar overlay. */
export const OVERLAY_GROUP_PREVIEW_LIMIT = 4

/** Catalog snapshot stays warm briefly; refetch on focus/remount recovers drift. */
export const GLOBAL_SEARCH_CATALOG_STALE_TIME_MS = 60_000
