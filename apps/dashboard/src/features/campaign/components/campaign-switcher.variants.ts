/** Dropdown panel — wider than the trigger so campaign names are not truncated. */
export const campaignSwitcherDropdownContentClasses =
  'w-max max-w-sm min-w-[max(18rem,var(--radix-dropdown-menu-trigger-width))]'
