/** Expanded catalog panel content — vertical rhythm only; horizontal inset comes from the body. */
export const equipmentPickerItemDetailsSectionClasses = 'space-y-4'

/** +8px over default section gap before purchase — pairs with quantity stepper shim below. */
export const equipmentPickerItemDetailsPurchaseSectionClasses = '!mt-6'
