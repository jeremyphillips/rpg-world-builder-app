/** `pl-2` compensates for body wash `-ml-2` bleed so copy aligns with the header column. */
export const equipmentPickerItemDetailsSectionClasses = 'space-y-4 pt-3 pl-2 pr-3 pb-3'

/** +8px over default section gap before purchase — pairs with quantity stepper shim below. */
export const equipmentPickerItemDetailsPurchaseSectionClasses = '!mt-6'
