import type { CharacterBuildContext } from '@rpg/contracts'

import { createPopulatedStandaloneBuilderContextFixture } from '../../../../lib/fixtures/character-builder-fixtures'

export const identityStepTestContext: CharacterBuildContext =
  createPopulatedStandaloneBuilderContextFixture()
