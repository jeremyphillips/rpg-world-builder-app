import { cva } from 'class-variance-authority'

export const characterRelationshipIndicatorVariants = cva(
  'size-4 shrink-0 text-foreground-subtle group-hover:text-foreground group-focus-within:text-foreground',
)
