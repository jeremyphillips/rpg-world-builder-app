/**
 * Modal body spacing:
 * - 16px between setup sections (summary ↔ summary ↔ expanded field)
 * - 16px between an expanded field's label and control
 */
export const createSetupModalBodyClasses = 'flex flex-col gap-4 [&_[data-field-align]]:gap-y-4'
