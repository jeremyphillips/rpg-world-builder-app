/** Bulleted reference lists nested inside bulk resolution rows. */
export const usageReferenceRowListClasses =
  'list-disc space-y-1 pl-5 text-xs [&_a]:text-xs' as const

/** Bulleted reference lists in single blocked dialogs (flat, outside rows). */
export const usageReferenceFlatListClasses =
  'list-disc space-y-2 pl-5 text-sm [&_a]:text-sm' as const
