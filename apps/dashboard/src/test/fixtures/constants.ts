import type { SystemRulesetId } from '@rpg/contracts'

/** Campaign id used by Storybook column stories and detail stories with router links. */
export const STORY_CAMPAIGN_ID = 'camp_1'

/** Ruleset for system catalog picks in dashboard Storybook fixtures. */
export const STORY_RULESET_ID = 'srd-cc-5.2.1' satisfies SystemRulesetId

/** Shared timestamp for content test fixtures. */
export const CONTENT_TIMESTAMP = '2026-01-01T00:00:00.000Z'
