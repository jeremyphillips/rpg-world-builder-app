import { cva } from 'class-variance-authority'

export const selectOptionItemContentVariants = cva('flex w-full items-center justify-between gap-3')

export const selectOptionItemReasonVariants = cva('text-muted-foreground text-xs')
