/**
 * Compact inline action hit targets — 24px height (`--control-action-compact-height`).
 * Shared by Button `density="compact"`, ContentCard heading/icon actions, and similar row controls.
 */
export const compactActionHeightClasses = 'h-control-action-compact'

export const compactActionSizeClasses = 'size-control-action-compact'

/** Icon glyph scale paired with compact action hit targets. */
export const compactActionIconGlyphClasses = '[&_svg]:size-3'
