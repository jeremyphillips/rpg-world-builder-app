# Public contracts (scaffold)

Reserved for marketing and CMS schemas used by the public app. Not re-exported
from the root `@rpg/contracts` barrel.

Import explicitly via `@rpg/contracts/public` when modules are added.
