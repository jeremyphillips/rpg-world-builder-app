// ---------------------------------------------------------------------------
// Heritage culture aliases — map species heritage picks to naming culture ids.
// ---------------------------------------------------------------------------

export type HeritageCultureAlias = {
  speciesId: string
  heritageId: string
  targetCultureId: string
}
