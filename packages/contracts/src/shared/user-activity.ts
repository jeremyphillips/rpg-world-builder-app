/** Recent-activity window for admin filters and queries. */
export const USER_RECENT_ACTIVITY_DAYS = 30

/** Minimum interval between persisted `lastActiveAt` writes per user. */
export const USER_ACTIVITY_WRITE_INTERVAL_MINUTES = 30
